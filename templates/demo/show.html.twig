{% extends 'base.html.twig' %}

{% block body %}
     <article>
        <h2>{{ article.title }}</h2>
        <div>Ecrit le {{article.createdAt | date('d,m,y')}} à {{article.createdAt | date('H:i')}}</div>
        <div class="content">
            <img src="{{article.image}}" alt="" />
            {{article.content | raw}}
        </div>
    </article>
{% endblock %}